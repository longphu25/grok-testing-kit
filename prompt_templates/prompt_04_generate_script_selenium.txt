/generate-automation-from-testcases

# CONTEXT
- Language: Java
- Test Framework: TestNG
- Automation Tool: Selenium WebDriver v4
- Design Pattern: Page Object Model (POM)
- Build tool: Maven

# SYSTEM
- URL: [System URL]
- Test account: [Use existing fixture / or EMAIL + PASSWORD]

# PROJECT (choose 1 of 2)

## A. Source code is available (EXISTING PROJECT)
- Project path: [path/to/project]
- Agent MUST read project structure, base classes, config before generating code
- Agent MUST comply with existing conventions (naming, folder, patterns)
- Agent MUST reuse BasePage, BaseTest, DriverFactory, Utils — DO NOT duplicate
- Agent MUST generate files in the correct location in the current project structure

## B. No project yet (NEW PROJECT)
- Agent self-scaffolds structure according to workflow`/generate-automation-framework`

# TEST CASES
[PASTE TEST CASES HERE — as a table or list of steps]

For example:
| TC ID | Title | Steps | Expected Result | Priorities |
|---|---|---|---|---|
| TC01 | Login successful | 1. Open the login page 2. Enter email 3. Enter password 4. Click Login | Dashboard displayed | P1 |

# OUTPUT FORMAT
- Page Object classes (Generate correct pages folder & comply with naming convention)
- Test classes (Inherit BaseTest, use TestNG annotations)
- Utilities / Data Generators (Reuse existing utils or add if needed)
- Artifact task.md (checklist of progress steps)
- Test run results (PASS/FAIL summary - test must PASS 2 times in a row)

# NOTES
- The agent automatically opens the browser, inspects the UI, and collects locators from the actual DOM
- Agent automatically runs test and auto-heal if FAIL (no user asked — Rule E3)
- If there is an existing project: the agent reads the code first → complies with existing patterns